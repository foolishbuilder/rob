name: Deploy Portal Dev

on:
  push:
    branches: [main]
    paths:
      - "portal/**"
      - "rob/**"
      - "scripts/**"
      - "deploy/scripts/deploy-portal-dev.sh"
      - "deploy/systemd/rob-portal-dev.service"
      - "requirements.txt"
      - "requirements-dev.txt"
      - ".github/workflows/deploy-portal-dev.yml"
  workflow_dispatch:
    inputs:
      deploy_ref:
        description: "Branch, tag, or commit SHA to deploy"
        required: false
        default: "main"

concurrency:
  group: deploy-portal-dev
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Run portal deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.WEBHOOK_DEV_HOST }}
          username: ${{ secrets.WEBHOOK_DEV_USER }}
          key: ${{ secrets.WEBHOOK_DEV_SSH_KEY }}
          port: ${{ secrets.WEBHOOK_DEV_PORT }}
          script: |
            set -e
            DEPLOY_BRANCH=main DEPLOY_REF="${{ github.event_name == 'workflow_dispatch' && inputs.deploy_ref || github.sha }}" /opt/rob-portal/deploy-portal-dev.sh
